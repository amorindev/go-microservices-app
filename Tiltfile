local_resource(
    'orders-srv',
    'go run ./orders-srv',
    deps=['orders-srv'],
)

local_resource(
    'payments-srv',
    'go run ./payments-srv',
    deps=['payments-srv'],
)


local_resource(
    'inventory-srv',
    'go run ./inventory-srv',
    deps=['inventory-srv'],
)

local_resource(
    'catalog-srv',
    'go run ./catalog-srv',
    deps=['catalog-srv'],
)

local_resource(
    'gateway',
    'go run ./gateway',
    deps=['gateway'],
)